<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dedyyarf.informasiabsensisiswa">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".app.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuWaliMurid" />
        <activity android:name=".LoginGuru" />
        <activity android:name=".InputAbsensi" />
        <activity android:name=".InputNilai" />
        <activity android:name=".RegistrasiWaliMurid" />
        <activity android:name=".MenuGuru" />
        <activity android:name=".Bantuan" />
        <activity android:name=".PilihanKelasAbsensi" />
        <activity android:name=".PilihanKelasNilai"></activity>
    </application>

</manifest>